@model IEnumerable<Everest_DVD_Store.Models.MembershipCategories>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TotalLoan)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TotalLoan)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MembershipCategory }) |
            @Html.ActionLink("Details", "Details", new { id=item.MembershipCategory }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MembershipCategory })
        </td>
    </tr>
}

</table>
