@model IEnumerable<Everest_DVD_Store.Models.DVDCategories>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AgeRestricted)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AgeRestricted)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DVDsCategory }) |
            @Html.ActionLink("Details", "Details", new { id=item.DVDsCategory }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DVDsCategory })
        </td>
    </tr>
}

</table>
