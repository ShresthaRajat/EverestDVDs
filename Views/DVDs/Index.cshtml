@model IEnumerable<Everest_DVD_Store.Models.DVDs>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DVDz.DVDStudio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DVDStudio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DVDProducer)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RunTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ReleaseDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DVDz.DVDStudio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DVDStudio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DVDProducer)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RunTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ReleaseDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DVDTitle }) |
            @Html.ActionLink("Details", "Details", new { id=item.DVDTitle }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DVDTitle })
        </td>
    </tr>
}

</table>
