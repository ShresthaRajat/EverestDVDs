@model IEnumerable<WebApplication1.Models.LoanViewModel>

@{
    Layout = "~/Views/Shared/Loggedin.cshtml";
}
@{
    ViewBag.Title = "Index";
}
<h4 class="page-title text-truncate text-dark font-weight-medium mb-1">Function 8</h4>
<hr />
<div class="d-flex align-items-center">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb m-0 p-0">
            <li class="breadcrumb-item"><a href="index.html" class="text-muted">Home</a></li>
            <li class="breadcrumb-item text-muted active" aria-current="page">Loans Table</li>
        </ol>
    </nav>

</div>

@*<div class="col-5 align-self-center">
        <div class="customize-input float-right">
            <select class="custom-select custom-select-set form-control bg-white border-0 custom-shadow custom-radius">
                <option selected>Aug 19</option>
                <option value="1">July 19</option>
                <option value="2">Jun 19</option>
            </select>
        </div>
    </div>*@
    </div>
<hr />
<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">List of all members with all their details and the total number of DVDs they currently have on loan and highlight any member who has more DVDs on loan than they are allowed.</h4>
                    <hr />
                    <hr />
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
 
                                <tr>
                            <thead>
                            <th>C Name</th>
                            <th>Name</th>
                            <th>Number Of Loans</th>
                            <th>Loan Status</th>
                            </thead>
                            </tr>


                            @foreach (var item in Model)
                            {
                        <tr>

                            @if (item.LoanStatus == "Too much DVD")
                            {
                                <td style="color:red;">
                                    @item.CName
                                </td>
                                <td style="color:red;">
                                    @item.Name
                                </td>
                                <td style="color:red;">
                                    @item.NumberOfLoans
                                </td>
                                <td style="color:red;">
                                    @item.LoanStatus
                                </td>
                            }
                            else
                            {
                                <td>
                                    @item.CName
                                </td>
                                <td>
                                    @item.Name
                                </td>
                                <td>
                                    @item.NumberOfLoans
                                </td>
                                <td>
                                    @item.LoanStatus
                                </td>
                            }               



                        </tr>
                            }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>








